#include <bits/stdc++.h>
using namespace std;

// int gcd(int a,int b){ return (a==0)?b:gcd(b%a,a); }

// __gcd(a,b)

int main()
{
    // cout << gcd(10, 0) << endl;
    // cout<<__gcd(10,0)<<endl;

    return 0;
}